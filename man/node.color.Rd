% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_kegg_pathview.R
\name{node.color}
\alias{node.color}
\title{Map numeric node data to a color gradient}
\usage{
node.color(
  plot.data = NULL,
  discrete = FALSE,
  limit = 1,
  bins = 10,
  both.dirs = TRUE,
  low = "green",
  mid = "gray",
  high = "red",
  na.col = "transparent",
  trans.fun = NULL
)
}
\arguments{
\item{plot.data}{Data frame from \code{\link{node.map}}; \code{NULL} returns \code{NULL}.}

\item{discrete}{Logical; treat values as discrete categories when \code{limit}/\code{bins} are integer-compatible.}

\item{limit}{Numeric scalar or \code{c(low, high)}; data range clamped before binning.}

\item{bins}{Integer; number of color bins.  Default 10.}

\item{both.dirs}{Logical; \code{TRUE} = diverging low–mid–high, \code{FALSE} = sequential mid–high.}

\item{low}{Character; gradient colors.}

\item{mid}{Character; gradient colors.}

\item{high}{Character; gradient colors.}

\item{na.col}{Character; color for \code{NA} nodes.  Default \code{"transparent"}.}

\item{trans.fun}{Function applied to values before color mapping (e.g. \code{log2}), or \code{NULL}.}
}
\value{
Matrix of hex color strings matching the data columns of \code{plot.data}, or \code{NULL}.
}
\description{
Converts a numeric summary matrix (one row per pathway node, one column per
sample/contrast) into an equally-shaped matrix of hex color strings by
binning values along a low–mid–high color gradient. Values outside
\code{limit} are clamped before binning. \code{NA} entries receive
\code{na.col}.
}
\details{
Adapted from pathview source:
https://code.bioconductor.org/browse/pathview/blob/RELEASE_3_22/R/node.color.R
}
