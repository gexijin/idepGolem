% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_kegg_pathview.R
\name{col.key}
\alias{col.key}
\title{Draw a color scale key on the active graphics device}
\usage{
col.key(
  discrete = FALSE,
  limit = 1,
  bins = 10,
  cols = NULL,
  both.dirs = TRUE,
  low = "green",
  mid = "gray",
  high = "red",
  graph.size,
  node.size,
  size.by.graph = TRUE,
  key.pos = "topright",
  off.sets = c(x = 0, y = 0),
  align = "n",
  cex = 1,
  lwd = 1
)
}
\arguments{
\item{discrete}{Logical; if \code{TRUE}, treat the scale as discrete integer
bins rather than a continuous gradient.}

\item{limit}{Numeric; either a single symmetric bound (e.g. \code{1}) or a
two-element vector \code{c(min, max)} defining the data range.}

\item{bins}{Integer; number of color bins in the key.}

\item{cols}{Optional character vector of colors overriding the default
low/mid/high gradient. Length must equal \code{bins}.}

\item{both.dirs}{Logical; if \code{TRUE} the scale spans negative to positive
(low → mid → high); if \code{FALSE} spans zero to positive (mid → high).}

\item{low}{Character; color for the low end of the scale (when
\code{both.dirs = TRUE}).}

\item{mid}{Character; color for the midpoint of the scale.}

\item{high}{Character; color for the high end of the scale.}

\item{graph.size}{Numeric vector \code{c(width, height)} of the graph in
pixels, used to scale key dimensions when \code{size.by.graph = TRUE}.}

\item{node.size}{Numeric vector \code{c(width, height)} of a representative
node in pixels, used to scale key dimensions when \code{size.by.graph = FALSE}.}

\item{size.by.graph}{Logical; if \code{TRUE}, key dimensions are derived from
\code{graph.size}; if \code{FALSE}, from \code{node.size}.}

\item{key.pos}{Character; corner position for the key. One of
\code{"topright"}, \code{"topleft"}, \code{"bottomright"}, \code{"bottomleft"}.}

\item{off.sets}{Named numeric vector \code{c(x = ..., y = ...)} of pixel
offsets accumulated from previously drawn keys, allowing stacking.}

\item{align}{Character; stacking direction when drawing multiple keys:
\code{"x"} (horizontal), \code{"y"} (vertical), or \code{"n"} (none).}

\item{cex}{Numeric; character expansion factor for key axis labels.}

\item{lwd}{Numeric; line width for tick marks drawn on the key.}
}
\value{
Named numeric vector \code{c(x, y)} of updated offset values, to be
  passed as \code{off.sets} when drawing a subsequent key.
}
\description{
Renders a labeled color gradient bar onto the current plot, typically used
to annotate the expression/fold-change color scale on KEGG pathway images.
Adapted from pathview source:
https://code.bioconductor.org/browse/pathview/blob/RELEASE_3_22/R/col.key.R
}
