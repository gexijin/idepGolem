% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_kegg_pathview.R
\name{mypathview}
\alias{mypathview}
\title{Render a KEGG pathway diagram with expression data overlaid}
\usage{
mypathview(
  gene.data = NULL,
  cpd.data = NULL,
  pathway.id,
  species = "hsa",
  kegg.dir = ".",
  cpd.idtype = "kegg",
  gene.idtype = "entrez",
  gene.annotpkg = NULL,
  min.nnodes = 3,
  kegg.native = TRUE,
  map.null = TRUE,
  expand.node = FALSE,
  split.group = FALSE,
  map.symbol = TRUE,
  map.cpdname = TRUE,
  node.sum = "sum",
  discrete = list(gene = FALSE, cpd = FALSE),
  limit = list(gene = 1, cpd = 1),
  bins = list(gene = 10, cpd = 10),
  both.dirs = list(gene = TRUE, cpd = TRUE),
  trans.fun = list(gene = NULL, cpd = NULL),
  low = list(gene = "green", cpd = "blue"),
  mid = list(gene = "gray", cpd = "gray"),
  high = list(gene = "red", cpd = "yellow"),
  na.col = "transparent",
  ...
)
}
\arguments{
\item{gene.data}{Named numeric vector/matrix (names = Entrez IDs) or character vector of IDs (binary presence).}

\item{cpd.data}{Named numeric vector/matrix of compound data (names = KEGG compound IDs), or \code{NULL}.}

\item{pathway.id}{Five-digit KEGG pathway ID without species prefix (e.g. \code{"04110"}).}

\item{species}{KEGG organism code (e.g. \code{"hsa"}, \code{"mmu"}).}

\item{kegg.dir}{Directory for caching KEGG KGML/PNG files and writing output.  Default \code{"."}.}

\item{cpd.idtype}{Identifier type for \code{cpd.data}.}

\item{gene.idtype}{Identifier type for \code{gene.data}.  Default \code{"entrez"}.}

\item{gene.annotpkg}{Bioconductor annotation package; auto-looked-up from \code{bods} when \code{NULL}.}

\item{min.nnodes}{Minimum mapped nodes required to render.  Default 3.}

\item{kegg.native}{Logical; \code{TRUE} renders the native KEGG PNG overlay.}

\item{map.null}{Logical; render even when no nodes are mapped.  Default \code{TRUE}.}

\item{expand.node, split.group}{Logical; expand multi-gene / split group nodes.  Both default \code{FALSE}.}

\item{map.symbol, map.cpdname}{Logical; map IDs to gene symbols / compound names for labels.  Both default \code{TRUE}.}

\item{node.sum}{Summary function when multiple molecules hit one node.  Default \code{"sum"}.}

\item{discrete, limit, bins, both.dirs, trans.fun}{Named lists (\code{gene}/\code{cpd}); control color-mapping per channel.}

\item{low, mid, high}{Named lists (\code{gene}/\code{cpd}); gradient colors per channel.}

\item{na.col}{Color for unmapped nodes.  Default \code{"transparent"}.}

\item{...}{Forwarded to \code{\link{my.keggview.native}}.}
}
\value{
Invisibly, a list with \code{plot.data.gene}, \code{plot.data.cpd}, and \code{node.data}.
  Side-effect: writes PNG(s) to \code{kegg.dir}.
}
\description{
Drop-in replacement for \code{pathview()} that writes output
images to a caller-specified directory (\code{kegg.dir}) instead of the R
working directory, and resolves all helper functions from the idepGolem
namespace rather than requiring the package. Only the
\code{kegg.native = TRUE} code path (PNG overlay) is fully supported; the
graph-based code path is present but not exercised by iDEP.
}
\details{
Adapted from pathview source:
https://code.bioconductor.org/browse/pathview/blob/RELEASE_3_22/R/pathview.R
}
