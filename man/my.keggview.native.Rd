% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_kegg_pathview.R
\name{my.keggview.native}
\alias{my.keggview.native}
\title{Render a KEGG native (PNG-based) pathway visualization}
\usage{
my.keggview.native(
  plot.data.gene = NULL,
  plot.data.cpd = NULL,
  cols.ts.gene = NULL,
  cols.ts.cpd = NULL,
  node.data,
  pathway.name,
  out.suffix = "pathview",
  kegg.dir = ".",
  multi.state = TRUE,
  match.data = TRUE,
  same.layer = TRUE,
  res = 400,
  cex = 0.25,
  discrete = list(gene = FALSE, cpd = FALSE),
  limit = list(gene = 1, cpd = 1),
  bins = list(gene = 10, cpd = 10),
  both.dirs = list(gene = TRUE, cpd = TRUE),
  low = list(gene = "green", cpd = "blue"),
  mid = list(gene = "gray", cpd = "gray"),
  high = list(gene = "red", cpd = "yellow"),
  na.col = "transparent",
  new.signature = TRUE,
  plot.col.key = TRUE,
  key.align = "x",
  key.pos = "topright",
  ...
)
}
\arguments{
\item{plot.data.gene}{Data frames from \code{\link{node.map}}, or \code{NULL}.}

\item{plot.data.cpd}{Data frames from \code{\link{node.map}}, or \code{NULL}.}

\item{cols.ts.gene}{Color matrices from \code{\link{node.color}} (rows = nodes, cols = samples), or \code{NULL}.}

\item{cols.ts.cpd}{Color matrices from \code{\link{node.color}} (rows = nodes, cols = samples), or \code{NULL}.}

\item{node.data}{Named list from \code{\link{node.info}}.}

\item{pathway.name}{Full KEGG pathway ID with species prefix (e.g. \code{"hsa04110"}).}

\item{out.suffix}{Suffix appended to the output filename.  Default \code{"pathview"}.}

\item{kegg.dir}{Directory of the cached PNG and output destination.  Default \code{"."}.}

\item{multi.state}{Logical; one PNG per sample column vs. single composite.  Default \code{TRUE}.}

\item{match.data}{Logical; align gene/compound color columns by name.  Default \code{TRUE}.}

\item{same.layer}{Logical; overlay colors directly on the PNG layer.  Default \code{TRUE}.}

\item{res}{Output PNG resolution in DPI.  Default 400.}

\item{cex}{Character expansion for node text.  Default 0.25.}

\item{discrete}{Named list (\code{gene}/\code{cpd}); passed to \code{\link{col.key}}.}

\item{limit}{Named list (\code{gene}/\code{cpd}); passed to \code{\link{col.key}}.}

\item{bins}{Named list (\code{gene}/\code{cpd}); passed to \code{\link{col.key}}.}

\item{both.dirs}{Named list (\code{gene}/\code{cpd}); passed to \code{\link{col.key}}.}

\item{low}{Named list (\code{gene}/\code{cpd}); passed to \code{\link{col.key}}.}

\item{mid}{Named list (\code{gene}/\code{cpd}); passed to \code{\link{col.key}}.}

\item{high}{Named list (\code{gene}/\code{cpd}); passed to \code{\link{col.key}}.}

\item{na.col}{Color for \code{NA} nodes.  Default \code{"transparent"}.}

\item{new.signature}{Logical; stamp the iDEP watermark.  Default \code{TRUE}.}

\item{plot.col.key}{Logical; draw the color-scale key.  Default \code{TRUE}.}

\item{key.align}{Alignment of the key; passed to \code{\link{col.key}}.}

\item{key.pos}{Corner position of the key; passed to \code{\link{col.key}}.}

\item{...}{Unused; retained for signature compatibility.}
}
\value{
Invisibly, the number of PNG files written.  Side-effect: writes annotated PNGs to \code{kegg.dir}.
}
\description{
Reads the reference PNG for a KEGG pathway from \code{kegg.dir}, overlays
colored rectangles on gene and compound nodes according to pre-computed
color matrices, optionally adds a color-scale key, stamps the iDEP
watermark, and writes one PNG per sample/contrast column to \code{kegg.dir}.
This is the rendering back-end called by \code{\link{mypathview}} when
\code{kegg.native = TRUE}.
}
\details{
Adapted from \code{keggview.native()}. Modified to resolve all
helper functions from the idepGolem namespace and to write output to a
caller-specified directory rather than the R working directory.
}
